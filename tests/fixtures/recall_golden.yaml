# Auto-generated by scripts/generate_golden_set.py
# Fully synthetic. No user content. No PHI. No real session IDs.
# Inputs: public CLX specs under /specs/*.md.
# Seed: 0xcafe
version: 1
source: synthetic-public-specs-only
seed: 51966
spec_sources:
  - "CLX Claude Code Plugin — Design Spec"
  - "Azure OpenAI Backend — Design Spec"
  - "Provider Fallback + Per-Project Config Override — Design Spec"
  - "CLX Comprehensive Test Plan — v2"
  - "CLX 0.8.0 — Memory, Skills, and Coverage Design"
snapshot_corpus:
  - snapshot_id: 1
    summary: "Reciprocal rank fusion replaces linear hybrid merge in recall."
    key_facts: "rrf, fusion, recall, hybrid"
  - snapshot_id: 2
    summary: "RecallEngine::query threads RRF config through the pipeline."
    key_facts: "rrf, engine, pipeline, query"
  - snapshot_id: 3
    summary: "Cormack et al. 2009 use k=60 as the literature-standard RRF constant."
    key_facts: "cormack, k, sigir, rrf"
  - snapshot_id: 4
    summary: "Cross-encoder bge-reranker-v2-m3 runs after RRF with a 250ms budget."
    key_facts: "reranker, cross-encoder, bge, latency"
  - snapshot_id: 5
    summary: "Multiplicative time-decay uses exp(-ln(2)*age_days/30) half-life."
    key_facts: "decay, half-life, time, score"
  - snapshot_id: 6
    summary: "Time-decay multiplies score by half-life curve before the gate."
    key_facts: "decay, gate, percentile, pipeline"
  - snapshot_id: 7
    summary: "Percentile gate replaces brittle 0.35 fixed similarity threshold."
    key_facts: "percentile, gate, threshold, recall"
  - snapshot_id: 10
    summary: "CLX plugin manifest registers skills under .claude/skills directory."
    key_facts: "plugin, manifest, skills, directory"
  - snapshot_id: 11
    summary: "SKILL.md frontmatter schema declares name, description, and triggers."
    key_facts: "skill, frontmatter, schema, yaml"
  - snapshot_id: 12
    summary: "Skill descriptions drive progressive disclosure and discovery."
    key_facts: "skill, description, discovery, progressive"
  - snapshot_id: 13
    summary: "Plugin skills are auto-discovered from the plugin's skills folder."
    key_facts: "plugin, autodiscovery, skills, folder"
  - snapshot_id: 14
    summary: "Plugin components: commands, agents, hooks, and skills compose the manifest."
    key_facts: "plugin, commands, agents, skills, hooks"
  - snapshot_id: 20
    summary: "CLX loads defaults via figment with env, file, and CLI layers."
    key_facts: "figment, config, defaults, layering"
  - snapshot_id: 21
    summary: "AutoRecallConfig exposes reranker_enabled, reranker_timeout_ms, rrf_k."
    key_facts: "autorecall, config, reranker, fields"
  - snapshot_id: 22
    summary: "rrf_enabled boolean lets operators roll back to linear merge."
    key_facts: "rrf, rollback, config, flag"
  - snapshot_id: 23
    summary: "Percentile gate default is 70 in AutoRecallConfig."
    key_facts: "percentile, gate, default, config"
  - snapshot_id: 24
    summary: "paths::model_cache_dir resolves under the CLX data directory for cached reranker weights."
    key_facts: "paths, model, cache, directory"
  - snapshot_id: 30
    summary: "UserPromptSubmit hook spawns background bge-reranker fetch on miss."
    key_facts: "hook, prompt, background, fetch"
  - snapshot_id: 31
    summary: "PreToolUse hook validates shell commands against the deny list."
    key_facts: "pretooluse, validation, shell, deny"
  - snapshot_id: 32
    summary: "PreToolUse hook blocks dangerous commands before execution."
    key_facts: "block, dangerous, shell, command"
  - snapshot_id: 33
    summary: "SessionEnd hook triggers the final auto-summary snapshot."
    key_facts: "sessionend, hook, auto-summary, snapshot"
  - snapshot_id: 34
    summary: "std::sync::Once guard prevents duplicate model fetch spawns."
    key_facts: "once, guard, fetch, dedup"
  - snapshot_id: 40
    summary: "TrustToken stores enabled_at, expires_at, and duration on disk."
    key_facts: "trust, token, expiry, disk"
  - snapshot_id: 41
    summary: "clx model fetch acquires a lockfile to prevent concurrent runs."
    key_facts: "fetch, lockfile, concurrency, model"
  - snapshot_id: 42
    summary: "Audit log captures actor, operation, and outcome for elevated calls."
    key_facts: "audit, log, fields, elevated"
  - snapshot_id: 43
    summary: "Secret redaction strips API key prefixes before audit log persistence."
    key_facts: "redaction, secret, audit, persistence"
  - snapshot_id: 44
    summary: "SecretString wraps backend credentials in zeroizing storage."
    key_facts: "secretstring, credentials, zeroize, backend"
  - snapshot_id: 50
    summary: "Storage::open applies schema migrations idempotently at startup."
    key_facts: "migration, schema, startup, idempotent"
  - snapshot_id: 51
    summary: "Schema v6 introduces tool_events for hook-driven tool tracking."
    key_facts: "schema, v6, tool_events, hook"
  - snapshot_id: 52
    summary: "audit_log foreign key references sessions with ON DELETE CASCADE."
    key_facts: "audit, foreign-key, cascade, session"
  - snapshot_id: 53
    summary: "snapshots_fts virtual table enables BM25 full-text search."
    key_facts: "fts5, virtual-table, bm25, snapshots"
  - snapshot_id: 54
    summary: "Schema version check refuses downgrade to prevent corruption."
    key_facts: "schema, version, downgrade, guard"
pairs:
  - id: q-001
    category: skills
    query: "How do skills declare their trigger description for progressive disclosure?"
    expected_snapshot_ids: [12, 14]
  - id: q-002
    category: migration
    query: "How does CLX guard against running against a newer schema version?"
    expected_snapshot_ids: [53, 54]
  - id: q-003
    category: trust
    query: "How does CLX redact secrets before writing audit log rows?"
    expected_snapshot_ids: [43, 44]
  - id: q-004
    category: config
    query: "How does CLX resolve the model cache directory under the user home?"
    expected_snapshot_ids: [23, 24]
  - id: q-005
    category: skills
    query: "How does the CLX plugin expose skills to Claude Code via the manifest?"
    expected_snapshot_ids: [10, 11]
  - id: q-006
    category: hook
    query: "How does the PreToolUse hook validate dangerous shell commands before execution?"
    expected_snapshot_ids: [31, 32]
  - id: q-007
    category: recall
    query: "How does the RRF rank fusion stage work in the recall pipeline?"
    expected_snapshot_ids: [1, 2]
  - id: q-008
    category: hook
    query: "How does the SessionEnd hook trigger the auto-summary snapshot path?"
    expected_snapshot_ids: [33, 34]
  - id: q-009
    category: migration
    query: "How does the audit_log foreign key constraint cascade on session delete?"
    expected_snapshot_ids: [52, 53]
  - id: q-010
    category: migration
    query: "How does the storage layer apply schema migrations on open?"
    expected_snapshot_ids: [50, 51]
  - id: q-011
    category: recall
    query: "How is the cross-encoder reranker integrated after RRF in the recall pipeline?"
    expected_snapshot_ids: [2, 4]
  - id: q-012
    category: config
    query: "How is the rrf_enabled flag wired through RecallEngine::query?"
    expected_snapshot_ids: [2, 22]
  - id: q-013
    category: trust
    query: "How is the trust mode token stored and expired on disk?"
    expected_snapshot_ids: [40, 41]
  - id: q-014
    category: trust
    query: "What audit log fields capture an elevated trust operation?"
    expected_snapshot_ids: [42, 43]
  - id: q-015
    category: skills
    query: "What does the SKILL.md frontmatter schema require for discoverability?"
    expected_snapshot_ids: [11, 12]
  - id: q-016
    category: hook
    query: "What does the UserPromptSubmit hook do when the reranker model is missing?"
    expected_snapshot_ids: [30, 31]
  - id: q-017
    category: config
    query: "What is the default percentile gate value in AutoRecallConfig?"
    expected_snapshot_ids: [21, 23]
  - id: q-018
    category: recall
    query: "What is the multiplicative time-decay formula and default half-life applied to recall hits?"
    expected_snapshot_ids: [5, 6]
  - id: q-019
    category: skills
    query: "What is the relationship between commands, agents, and skills in the CLX plugin?"
    expected_snapshot_ids: [10, 14]
  - id: q-020
    category: recall
    query: "What value of k does Cormack et al. 2009 recommend for reciprocal rank fusion?"
    expected_snapshot_ids: [1, 3]
  - id: q-021
    category: migration
    query: "What was the schema change that added the snapshots_fts virtual table?"
    expected_snapshot_ids: [50, 53]
  - id: q-022
    category: config
    query: "Where are CLX configuration defaults loaded from and how does figment layer overrides?"
    expected_snapshot_ids: [20, 21]
  - id: q-023
    category: trust
    query: "Where does CLX persist the SecretString for backend credentials?"
    expected_snapshot_ids: [40, 44]
  - id: q-024
    category: config
    query: "Which AutoRecallConfig fields control the reranker timeout and enabled flag?"
    expected_snapshot_ids: [21, 22]
  - id: q-025
    category: skills
    query: "Which directory holds plugin-provided skills and how are they discovered?"
    expected_snapshot_ids: [10, 13]
  - id: q-026
    category: hook
    query: "Which hook fires the background fetch for bge-reranker-v2-m3 on first run?"
    expected_snapshot_ids: [30, 33]
  - id: q-027
    category: trust
    query: "Which lockfile prevents concurrent clx model fetch invocations?"
    expected_snapshot_ids: [41, 42]
  - id: q-028
    category: migration
    query: "Which migration introduced the tool_events table in schema v6?"
    expected_snapshot_ids: [51, 52]
  - id: q-029
    category: hook
    query: "Which std::sync::Once guard prevents repeated background model fetch spawns?"
    expected_snapshot_ids: [30, 34]
  - id: q-030
    category: recall
    query: "Why does the recall pipeline use a percentile gate instead of a fixed score threshold?"
    expected_snapshot_ids: [6, 7]
